{
  "name": "gluedle",
  "version": "0.1.0",
  "private": true,
  "description": "An audio-free daily song metadata guessing game and GLUE concept site.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview",
    "sync:origin-config": "node scripts/sync-song-origin-overrides.mjs",
    "check": "npm run lint && npm run test && npm run build"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "vite": "8.2.0"
  }
}
